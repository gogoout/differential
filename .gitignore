tmp/
/target
*.local.toml

# The demo recording. 2.8 MB, and GitHub will not play a video from a repo
# path anyway — it is uploaded as a GitHub attachment and linked from the
# README. `assets/record.vhs` is what reproduces it.
assets/demo.webm
